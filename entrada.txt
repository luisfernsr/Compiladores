int soma(int a, int b) {
  int resultado = a + b;
  return resultado;
}

int main() {
  int x = 10;
  int y = 20;
  int total = soma(x, y);
  string nome = "Ana";
  char letra = 'A';
  bool ativo = true;

  if (total > 20) {
    total = total - 1;
  } else {
    total = total + 1;
  }

  while (x < y) {
    x = x + 1;
  }

  return 0;
}
